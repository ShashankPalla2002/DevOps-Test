apiVersion: v1

kind: Service

metadata:
  name: {{ .Values.name }}
  labels:
{{ .Values.labels | toYaml | indent 4 }}

spec:
  type: {{ .Values.service.type }}
  selector: 
{{ .Values.labels | toYaml | indent 4 }}
  ports:
{{ .Values.service.ports | toYaml | indent 6 }}